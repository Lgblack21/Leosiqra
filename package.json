{
  "name": "leosiqra-cloudflare-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix cloudflare-project run dev",
    "deploy": "npm --prefix cloudflare-project run deploy",
    "check": "npm --prefix cloudflare-project run check",
    "db:migrate": "npm --prefix cloudflare-project run db:migrate",
    "db:migrate:remote": "npm --prefix cloudflare-project run db:migrate:remote",
    "data:import": "npm --prefix cloudflare-project run data:import",
    "r2:upload": "npm --prefix cloudflare-project run r2:upload"
  }
}
